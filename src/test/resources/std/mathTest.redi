import math

fun main() {
    testSolve2()
}

fun testSolve2() {
    test(solveSquare(0, 0) == [0.0])
    log(solveSquare(1, 0).sorted())
    test(solveSquare(1, 0).sorted() == [-1.0, 0.0])
    test(solveSquare(0, 1) == [])
    test(solveSquare(2, 1) == [-1.0])
    test(solveSquare(0, -1).sorted() == [-1.0, 1.0])
}

fun testSolve3() {
    test(solve3())
}