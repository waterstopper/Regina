// rnd() - returns random value from 0 (inclusive) to 1 (exclusive). No declaration for that
// sin(), cos()
// int(), str(), double()

seed = 42 - seed of rand

fun randFromList(list) {

}

fun rotate(point, angle) {
}
